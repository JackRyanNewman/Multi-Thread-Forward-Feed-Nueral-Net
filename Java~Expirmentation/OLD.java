//Jack Newman
//Date: 2024-12-4: Assigment 3. 

public class OLD {
//	if(P) System.out.println("Selected parallel mode, note the program may error, if make a large nueral net which will cause memory problems.\n");
//	if(HPTIter > 0 && !fileName.equals("")) HPTIter = 0;
//	if(HPTIter > 0 || !fileName.equals("")) {
//		if(HPTIter == 0) createBseSet(fileName, r, v); //inits data, and minimax scales it.
//		if(bDiv>0) btchSz = ((int) (BseSet.length * .8)) / bDiv;
//		if(btchSz==0 || bDiv==0 || btchSz > RegSet.length) btchSz = (int) (BseSet.length * .8);
//		int[][] ret = initMB(btchSz, RegSet.length);
//		NeuralNet neuralNet = new NeuralNet(hiddenLayers, initWE, alpha, lambda, ret[2][0], IOLen, OPLen, v, AF, P); //Creates my nueral net objects nessarcy for training. 
//		MGDController(neuralNet, ret[0], ret[1], alpha, lambda, epochLimit, btchSz, r, v);							
//	}
//	else System.out.print("The file name was incorrect");
//	HPTIter++;
//} 
	
}
